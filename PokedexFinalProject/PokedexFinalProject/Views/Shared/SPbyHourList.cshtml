@model IEnumerable<PokedexFinalProject.Models.SPbyHourViewModel>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hour1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hour2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hour1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hour2)
        </td>
  
    </tr>
}

</table>
